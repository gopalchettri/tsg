"""Live threat-intel layer: open feeds (CISA KEV, ICS advisories, OTX, URLhaus,
TAXII servers) cached in the Mongo `threat_intel` collection and surfaced as
fail-open enrichment context during scenario generation."""
