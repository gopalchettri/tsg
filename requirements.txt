# Generated from pyproject.toml's [project.dependencies] — keep in sync manually.
# For local development, `pip install -e ".[dev]"` (pyproject.toml) is still the
# canonical install path; this file exists for tooling that specifically wants
# requirements.txt (e.g. a Docker build step or a platform that doesn't read
# pyproject.toml).

fastapi>=0.141.1
uvicorn[standard]>=0.52.1
sse-starlette>=3.4.6
celery[redis]>=5.6.3
redis>=4.5.2,<6.5,!=4.5.5,!=5.0.2
gevent>=26.7.0
sqlalchemy>=2.0.51
pyodbc>=5.3.0
pydantic>=2.13.4
pydantic-settings>=2.14.2
pyjwt[crypto]>=2.13.0
litellm>=1.95.0
numpy>=2.4.1
pymongo>=4.17.0
structlog>=26.1.0
flower>=2.0.1
httpx>=0.28.1
mypy>=2.3.0
ruff>=0.16.1
taxii2-client>=2.3
tenacity>=9.1.4
pyyaml>=6.0.3
sentence-transformers>=5.0   # 5.x for get_embedding_dimension() — see local_models.py::_embedder

# Optional extras (pyproject.toml's [project.optional-dependencies]) — install
# with `pip install -e ".[dev]"` / ".[local]"` / ".[prod]"`, not via this file:
#   dev:   pytest>=8.2 (httpx/ruff/mypy are already top-level deps above)
#   dev: sentence-transformers>=5.0   (local embedding/reranker provider)
#   prod:  gunicorn>=22.0               (production process manager for the API)
